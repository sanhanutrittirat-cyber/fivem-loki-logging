{
  "name": "fivem-logwatch",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:api": "pnpm --filter @logwatch/api dev",
    "dev:web": "pnpm --filter @logwatch/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": { "node": ">=20" }
}
